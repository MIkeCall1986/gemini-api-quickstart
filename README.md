# Gemini API Quickstart - Python

This repository contains a simple Python Flask App running with the Google AI Gemini API, designed to get you started building with Gemini's multi-modal capabilities. The app comes with a basic UI and a Flask backend.

<img width="1271" alt="Screenshot 2024-05-07 at 7 42 28 AM" src="https://github.com/logankilpatrick/gemini-api-quickstart/assets/35577566/156ae3e0-cffa-47a3-8a71-1bded78c4632">

## Basic request

To send your first API request with the [Gemini API Python SDK](https://github.com/google-gemini/generative-ai-python), make sure you have the right dependencies installed (see installation steps below) and then run the following code:

```python
import os
import google.generativeai as genai

genai.configure(os.getenv("GEMINI_API_KEY"))
model = genai.GenerativeModel('gemini-1.5-flash')

chat = model.start_chat(history=[])
response = chat.send_message("In one sentence, explain how AI works to a child.")
print(response.text)

response = chat.send_message("Now explain it like I a Physics PhD student")
print(response.text)
```

## Setup

18.02.26
Ось результати аналізу та стратегія трансформації для проекту **Gemini API Quickstart**, підготовлені у форматі для копіювання в Notion.

---

# 📑 Звіт AI-консультанта: Проект "Gemini API Quickstart"

Цей проект є базовим шаблоном для швидкого розгортання додатків на базі Google AI Gemini API з використанням Python та Flask.

## 🧬 Частина 1: "ДНК" Проекту

Логіку коду проекту можна розбити на такі **атомарні функції**:

*   **Конфігурація та авторизація:** Налаштування зв'язку з API за допомогою ключа, отриманого з системних змінних оточення (`GEMINI_API_KEY`).
*   **Ініціалізація моделі:** Створення об'єкта генеративної моделі (за замовчуванням `gemini-1.5-flash`), яка підтримує мультимодальні можливості.
*   **Управління сесіями (Chat Logic):** Запуск чату з підтримкою історії повідомлень для ведення контекстуального діалогу.
*   **Обробка запитів та відповідей:** Функція відправки повідомлень моделі та отримання текстового результату в реальному часі.
*   **Веб-інтерфейс (Frontend/Backend Bridge):** Використання Flask для створення бекенду та рендерингу базового UI через шаблони HTML/CSS/JS.

### 💎 Головна технічна цінність
Головна технічна цінність полягає у **максимально низькому порозі входу** для використання мультимодальних можливостей Gemini. Проект надає готовий до роботи каркас (boilerplate), який дозволяє перейти від "чистого" Python-скрипта до працюючого веб-інтерфейсу менш ніж за 5 хвилин.

---

## 🚀 Частина 2: "Трансформація" (Сценарій сервісу)

Хоча цей проект вже використовує Gemini, ми можемо трансформувати його з "демонстраційного додатка" на **інтелектуальний шлюз для вашого сайту**.

### Як зміниться функціонал?
1.  **Глибока інтеграція з GitHub Spark:** Використання **GitHub Spark** дозволить миттєво розгорнути цей додаток як повноцінний інтелектуальний сервіс, що масштабується.
2.  **Мультимодальний аналіз даних:** Завдяки моделі `gemini-1.5-flash` сервіс зможе обробляти не лише текст, а й зображення або технічні логі, які завантажують користувачі вашого сайту.
3.  **Порівняння промптів (GitHub Models):** Інтеграція з **GitHub Models** дозволить тестувати та обирати найкращі інструкції (prompts) для ШІ, щоб підвищити якість відповідей на вашому сайті.

### Сценарій сервісу "Smart Data Interpreter" (Quickstart + Gemini + ID_{$})

Уявімо створення сервісу автоматичного аналізу технічних звітів на вашому сайті:
1.  **Ввід:** Користувач завантажує сирі дані або лог-файли через інтерфейс Flask.
2.  **Оркестрація (ID_{$}):** Ваш базовий Python-скрипт `ID_{$}` автоматично очищує ці дані та структурує їх.
3.  **Аналіз (Gemini):** Додаток передає структуровані дані до Gemini через SDK. Модель готує пояснення "людською мовою" та виділяє критичні помилки.
4.  **Результат:** Оброблена та проаналізована інформація повертається користувачу на сайті у вигляді інтерактивного дашборду.
5.  **Безпека:** Використання **GitHub Advanced Security** у робочому процесі гарантує, що ваші ключі API та дані користувачів захищені від витоків.

---

## 📋 План дій для Notion
| Крок | Дія | Результат |
| :--- | :--- | :--- |
| **1** | Клонування репозиторію та `pip install -r requirements.txt` | Робоче локальне середовище |
| **2** | Налаштування ключа в `.env` | Доступ до потужностей Gemini |
| **3** | Підключення ваших скриптів `ID_{$}` до `app.py` | Автоматизація бізнес-логіки |
| **4** | Розгортання через **GitHub Spark** | Готовий до використання сервіс на сайті |

---

### 💡 Резюме

**Суть:** **Швидкий старт із Gemini API через Python**.

**AI-Роль:** **Створення інтелектуальних застосунків через Spark**.

1. If you don’t have Python installed, install it [from Python.org](https://www.python.org/downloads/).

2. [Clone](https://docs.github.com/en/repositories/creating-and-managing-repositories/cloning-a-repository) this repository.

3. Create a new virtual environment:

   - macOS:
     ```bash
     $ python -m venv venv
     $ . venv/bin/activate
     ```

   - Windows:
     ```cmd
     > python -m venv venv
     > .\venv\Scripts\activate
     ```

   - Linux:
      ```bash
      $ python -m venv venv
      $ source venv/bin/activate
      ```

4. Install the requirements:

   ```bash
   $ pip install -r requirements.txt
   ```

5. Make a copy of the example environment variables file:

   ```bash
   $ cp .env.example .env
   ```

6. Add your [API key](https://ai.google.dev/gemini-api/docs/api-key) to the newly created `.env` file or as an environment variable.

7. Run the app:

```bash
$ flask run
```

You should now be able to access the app from your browser at the following URL: [http://localhost:5000](http://localhost:5000)!
